Install dotfiles

```shell
./install.py
```

Install Brewfile

```shell
brew bundle install
```

If on Mac, run this to make `/usr/libexec/java_home` function

```shell
sudo ln -sfn /opt/homebrew/opt/openjdk/libexec/openjdk.jdk /Library/Java/JavaVirtualMachines/openjdk.jdk
```
